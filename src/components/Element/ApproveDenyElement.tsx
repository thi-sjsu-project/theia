import type { ReactNode } from 'react';

type ApproveDenyElementProps = {
  message: string;
  children: ReactNode;
};

const ApproveDenyElement = ({
  message,
  children,
}: ApproveDenyElementProps) => {};

export default ApproveDenyElement;
